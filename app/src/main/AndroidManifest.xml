<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".SkinDiseaseCamApplication"
        android:theme="@style/Theme.Skin_Disease_Cam"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="36">

        <!--Main Activity-->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.BottomNavActivity" />

        <activity android:name=".activities.RecordsActivity" />

        <activity android:name=".activities.ViewResultsActivity" />

        <activity android:name=".activities.LibraryActivity" />

        <activity android:name=".activities.CameraActivity" />

        <activity android:name=".activities.CropImageActivity" />

        <activity android:name=".activities.ResultsActivity" />

        <activity android:name=".activities.PreviewImageActivity" />


        <!--Library Skin Diseases -->

        <activity android:name=".activities.LibraryAtopicDermActivity"></activity>

        <activity android:name=".activities.LibraryImpetigoActivity"></activity>

        <activity android:name=".activities.LibrarySarcoptesScabieiActivity"></activity>

        <activity android:name=".activities.LibraryWartsActivity"></activity>

        <activity android:name=".activities.LibraryTineaInfectionActivity"></activity>

        <activity android:name=".activities.LibraryPricklyHeatActivity"></activity>


    </application>

</manifest>